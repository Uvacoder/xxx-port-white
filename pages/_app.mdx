import 'nextra-theme-blog/style.css'
import '../styles/main.css'
import PlausibleProvider from 'next-plausible'

export default function Nextra({ Component, pageProps }) {
  return (
    <PlausibleProvider domain="about.0x8c.org">
      <Component {...pageProps} />
    </PlausibleProvider>
  )
}